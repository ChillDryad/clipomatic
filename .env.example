# -----------------------------------------------------------------------
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit your actual API key.
# -----------------------------------------------------------------------

# --- LLM (required) ---
# Any OpenAI-compatible endpoint works: OpenAI, Ollama, LiteLLM, Groq, etc.
#
# OpenAI:
#   LLM_BASE_URL=https://api.openai.com/v1
#   LLM_API_KEY=sk-...
#   LLM_MODEL=gpt-4o-mini
#
# Ollama local-only models (no API key needed):
#   LLM_BASE_URL=http://ollama:11434/v1   ← Docker service DNS
#   LLM_BASE_URL=http://localhost:11434/v1 ← when running app outside Docker
#   LLM_API_KEY=ollama                     ← any non-empty string, local ignores it
#   LLM_MODEL=llama3.2
#
# Ollama cloud/authenticated models (e.g. nemotron-3-super:cloud):
#   The app always points at the LOCAL Ollama daemon — it proxies cloud requests.
#   OLLAMA_API_KEY is loaded directly into the Ollama container via env_file,
#   so the daemon can authenticate with the cloud on your behalf.
#   LLM_BASE_URL=http://ollama:11434/v1    ← still the local daemon
#   LLM_API_KEY=<your-ollama-api-key>      ← sent as bearer token to local Ollama /v1
#   OLLAMA_API_KEY=<your-ollama-api-key>   ← must match LLM_API_KEY; used by Ollama daemon for cloud auth
#   LLM_MODEL=nemotron-3-super:cloud
LLM_BASE_URL=http://ollama:11434/v1
LLM_API_KEY=ollama
OLLAMA_API_KEY=ollama
LLM_MODEL=llama3.2

# --- Whisper (optional overrides) ---
WHISPER_MODEL=base
# auto = detects CUDA → Apple Silicon → CPU at runtime
# Override to "cuda" or "cpu" to force a specific backend
WHISPER_DEVICE=auto

# --- App ---
WORKSPACE_DIR=/app/workspace

# --- Database ---
# Default: SQLite (file-based, zero setup)
# For production with PostgreSQL:
#   DATABASE_URL=postgresql+asyncpg://user:password@host:5432/momiji
DATABASE_URL=sqlite+aiosqlite:///./momiji.db

# --- YouTube OAuth ---
# Create credentials at https://console.cloud.google.com/
# Enable YouTube Data API v3, add OAuth consent screen, create Web Application credentials
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret
# Must match the redirect URI registered in Google Cloud Console
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:7860/api/oauth/youtube/callback

# --- TikTok OAuth ---
# Register at https://developers.tiktok.com/ — Content Posting API access is gated
TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret
TIKTOK_OAUTH_REDIRECT_URI=http://localhost:7860/api/oauth/tiktok/callback

# --- Instagram/Facebook OAuth ---
# Create an app at https://developers.facebook.com/
# Requires Instagram Business account linked to a Facebook Page
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
FACEBOOK_OAUTH_REDIRECT_URI=http://localhost:7860/api/oauth/instagram/callback
