* embedded thumbnail image (user provides image, otherwise let youtube figure it out.)
* add preview player
* preview renders inaccurately, should render a preview EXACTLY how we will render the video (including facecam zoom & positioning)