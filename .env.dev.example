# Development environment variables
# Copy to .env.dev and customize as needed

# --- LLM (points to shared Ollama) ---
LLM_BASE_URL=http://ollama-shared:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=llama3.1:8b

# --- Vision analysis (optional — uses same LLM endpoint by default) ---
# Model must support vision (image input). Recommended: llava:13b, llama3.2-vision
# If unset, falls back to LLM_MODEL. Set VISION_MODEL=none to disable.
VISION_MODEL=llava:13b
# Seconds between sampled frames (lower = more frames = more analysis + cost)
VISION_SAMPLE_INTERVAL=30

# --- Whisper ---
WHISPER_MODEL=base
WHISPER_DEVICE=auto

# --- Workspace ---
WORKSPACE_DIR=/app/workspace

# --- Auth secrets (development) ---
JWT_SECRET=dev-secret-change-in-prod
OAUTH_ENCRYPTION_KEY=ZmFrZS1rZXktZm9yLWRldmVsb3BtZW50LW9ubHk=

# --- Database ---
DATABASE_URL=sqlite+aiosqlite:///./momiji.db

# --- OAuth (localhost for development) ---
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:7861/api/oauth/youtube/callback

TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret
TIKTOK_OAUTH_REDIRECT_URI=http://localhost:7861/api/oauth/tiktok/callback

FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
FACEBOOK_OAUTH_REDIRECT_URI=http://localhost:7861/api/oauth/instagram/callback
